server:
  port: 8002
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 1qaz@WSX
    url: jdbc:mysql://101.36.160.140:21029/xq?useSSL=false&useUnicode=true&characterEncoding=utf-8&serverTimezone=GMT
    hikari:
      minimum-idle: 5
      maximum-pool-size: 20
      idle-timeout: 30000
      max-lifetime: 1800000
      connection-timeout: 60000
      validation-timeout: 3000
      connection-test-query: select 1
      auto-commit: true
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  thymeleaf:
    cache: false
    mode: html
  servlet:
    multipart:
      #location: /tmp
      max-file-size: 2MB
      max-request-size: 10MB
file:
  staticAccessPath: /upload/**
  uploadFolder: /Users/chenchencui/Downloads/upload/


#开启日志 如果级别：off(少)>error>warn>info>debug(多)>all
logging:
  level:
    root: info
